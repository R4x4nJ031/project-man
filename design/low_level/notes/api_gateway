It MUST do

Accept all external HTTP/REST requests

Authenticate the request

Resolve user identity

Resolve tenant context

Attach security context to the request

Route request to the correct internal service

Enforce consistent error handling

Trigger audit logging for gateway-level events

It MUST NEVER do

Business logic

Authorization decisions

Database access

Permission evaluation

Tenant guessing from client input
